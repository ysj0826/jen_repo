server:
  port: 7777

spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    # docker-compose용
    # url: jdbc:mariadb://mariadb-persist:3306/calcdb
    #kubernetes용(section03)
    url: jdbc:mariadb://mariadb003ser:3310/calcdb
    username: root
    password: root1234

  # create: ??? ?? ??? ? ?? ???? drop?? ?? create
  # update: ??? ?? ??? ? ?? ???? ??? create, ??? ?? ?? ??? alter
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect